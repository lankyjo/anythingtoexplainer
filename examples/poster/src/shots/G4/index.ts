import type {ShotDef} from '../../common';
// Group G4: shot table. Array order is layer order. Frame ranges come from storyboard.md and
// script/timeline.md. Shot components: const N = useCurrentFrame() + F0 (F0 = this shot's from);
// primitives from '../../ui', easing/shared layer from '../../common'.
export const SHOTS_G4: ShotDef[] = [];
